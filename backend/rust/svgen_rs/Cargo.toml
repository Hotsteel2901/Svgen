[package]
name = "svgen_rs"
version = "1.0.0"
edition = "2021"
description = "SVGen native rasterization engine (fills polygons/rects, gradients, blending, downsampling)"
license = "MIT"

[lib]
name = "svgen_rs"
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
